---
name: update available modules

on:
  repository_dispatch:
    types: [update-modules]
  workflow_dispatch:

jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - name: print message
        run: | 
          echo "hello i am working"
      - name: Print config path
        run: |
          echo "Config path: ${{ github.event.client_payload.config-path }}"
